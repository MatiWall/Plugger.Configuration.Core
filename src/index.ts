import { AppConfig, createAppConfig } from "./AppConfig";
import {ConfigSchema, ConfigType} from "./schema";
export {
    ConfigSchema,
    
    AppConfig,
    createAppConfig,
}

export type {
    ConfigType
}